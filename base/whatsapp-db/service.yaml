apiVersion: v1
kind: Service
metadata:
  name: whatsapp-db
  labels:
    app: whatsapp-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: whatsapp-db
