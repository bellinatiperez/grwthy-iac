apiVersion: apps/v1
kind: Deployment
metadata:
  name: grwthy-web
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: web
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "200m"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grwthy-web
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: grwthy-dev-redirect-https@kubernetescrd
spec:
  tls:
    - hosts:
        - dev.grwthy.com
      secretName: grwthy-web-tls
  rules:
    - host: dev.grwthy.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grwthy-web
                port:
                  number: 3000
